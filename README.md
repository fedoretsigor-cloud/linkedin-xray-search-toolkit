# LinkedIn X-Ray Search Toolkit

Clean rebuild starting point.

The previous project state is archived in the `legacy` branch.

This `main` branch is intentionally reset so the search product can be rebuilt from a clear contract and tests.
